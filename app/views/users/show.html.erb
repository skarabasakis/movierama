<div id="movie-list-wrapper">
  <div id="sidebar">
    <%= render 'sidebar' %>
  </div>
  <div id="movie-list">
    <h3>Movies posted by <%= @user.username %></h3>
    <%= render 'movies_sorter' %>
    <%= render(@user_movies) || 'No movies to display' %>
  </div>
</div>